<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="tab_text" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:textSize">@dimen/txt_body2_menu_size</item>
        <item name="android:fontFamily">@font/rubik_medium</item>
    </style>

    <!-- Base application theme. -->
    <style name="toolbar_transparent" parent="Theme.AppCompat.Light">
        <item name="android:textColorPrimary">@color/primaryTextColor</item>
        <item name="colorPrimary">@android:color/transparent</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Base application theme. -->
    <style name="button.punxMusic" parent="Widget.AppCompat.Button">
        <item name="android:colorButtonNormal">@color/colorAccent</item>
        <item name="android:background">@color/colorAccent</item>
        <item name="android:elevation">0dp</item>
        <item name="android:foreground">?attr/selectableItemBackground</item>
        <item name="android:colorActivatedHighlight">@android:color/background_light</item>
        <item name="android:textColor">@color/lightTextColor</item>
        <item name="android:textSize">@dimen/txt_button_size</item>
        <item name="android:fontFamily">@font/rubik_bold</item>
    </style>

    <style name="txt.tittle" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/primaryTextColor</item>
        <item name="android:textSize">@dimen/txt_title_size</item>
        <item name="android:fontFamily">@font/rubik_bold</item>
        <item name="android:layout_marginBottom">@dimen/component_space_bottom_small</item>
    </style>





</resources>
